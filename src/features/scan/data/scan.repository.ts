import { scanMockRepository } from "@/features/scan/data/scan.mock-repository";
import { scanStorageRepository } from "@/features/scan/data/scan.storage";

export const scanProductRepository = scanMockRepository;
export const scanStorage = scanStorageRepository;
